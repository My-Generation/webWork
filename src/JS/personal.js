function update(){
    let data = prompt("请输入修改内容:");
    let before = document.getElementById("contain");
    before.innerText = data;
}

function update1(){
    let data = prompt("请输入修改内容:");
    let before = document.getElementById("contain1");
    before.innerText = data;
}

function update2(){
    let data = prompt("请输入修改内容:");
    let before = document.getElementById("contain2");
    before.innerText = data;
}